<% provide(:title, @user.name) %>
<div class="row">
    <div class="span4">

    <section>
    <% if @user.microposts.any? %>
    </section>

        <section>
    <%= render 'follow_form' if signed_in? %>
    </section>
    <div class="row">
        <h2><%= link_to @user.name %> - <%=  @user.course %></h2>
      </div>
    

<div class="span4">
    <div class="row">
      <h2> Conditions Updates (<%= @user.microposts.count %>)</h2>
    </div>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
      <%= will_paginate @microposts %>
    <% end %>
  </div>
      <div class="row">
    <%= render 'shared/boilerplate_conditions_feed' %>
    </div>
    </div>
</div>